# CreatorSpace 需求文档

版本: v0.3 业务规则确认稿
日期: 2026-06-16
状态: 核心技术、第一阶段边界与关键业务规则已确认

## 1. 项目定位

CreatorSpace 是一个基于 Spring Boot 与 Vue 3 的个人主题风格博客与创意作品展示平台。它不只承担文章发布功能，还要作为个人品牌展示、创意项目陈列、内容管理和数据化运营的综合内容系统。

平台核心方向是“内容系统”，不是单一博客 CRUD。系统需要同时支持文章、作品集、主题配置、创意墙、评论互动、访问统计、文件资源、搜索和后台 CMS 管理。

## 2. 技术基线

### 2.1 已确认技术栈

| 层级 | 技术 |
| --- | --- |
| 后端 | Spring Boot 4.0.x |
| Java | Java 21 |
| 前端 | Vue 3 + TypeScript |
| 构建工具 | Vite |
| Node.js | Node 24 |
| 数据库 | PostgreSQL |
| 缓存 | Redis |
| 数据访问 | MyBatis-Plus + 分页插件 |
| 后台 UI | Google Material Design 3 设计语言 |
| 鉴权 | JWT |
| 文件存储 | 本机本地存储, 路径相对项目根目录 |
| 搜索 | PostgreSQL `pg_trgm` + GIN 索引优先 |
| 前端结构 | 单个 Vue 3 应用承载前台与后台 |
| AI 预留 | 智谱 AI 免费多模态模型 |
| 部署方案 | 后续独立设计 Docker 方案 |

### 2.2 当前本机 / 容器环境快照

以下版本来自当前开发机与已运行 Docker 容器的只读检查:

| 服务 | 当前版本 |
| --- | --- |
| Java | 21.0.7 LTS |
| Node.js | 24.14.0 |
| npm | 11.9.0 |
| PostgreSQL | 17.9, 镜像 `pgvector/pgvector:pg17` |
| Redis | 7.4.8, 镜像 `redis:7-alpine` |

### 2.3 官方资料校验

- Spring Boot 当前官方系统要求页面显示 Spring Boot 4.x 需要 Java 17 及以上，当前选择 Java 21 LTS 可满足要求。参考: https://docs.spring.io/spring-boot/system-requirements.html
- Spring Boot 4.0 发布说明提到其基于新一代 Spring 生态、支持 Java 25，同时保留 Java 17 兼容性。参考: https://spring.io/blog/2025/11/20/spring-boot-4-0-0-available-now
- Vite 官方文档要求 Node.js `20.19+` 或 `22.12+`，当前 Node 24 满足要求。参考: https://vite.dev/guide/
- Vue 官方快速开始同样要求 Node.js `^20.19.0 || >=22.12.0`。参考: https://vuejs.org/guide/quick-start
- MyBatis-Spring 4.0 对应 Spring Framework 7.0+ 和 Java 17+，与 Spring Boot 4.x 的 Spring 7 基线匹配。参考: https://mybatis.org/spring/
- MyBatis-Plus 官方安装文档提示 3.5.9+ 开始插件部分改为可选依赖，后续接入分页插件时需要显式选择对应插件支持模块。参考: https://baomidou.com/en/getting-started/install/
- Material Design 3 是 Google 当前开放设计系统，后台 CMS 视觉与交互规范按该体系设计。参考: https://m3.material.io/
- PostgreSQL `pg_trgm` 官方文档说明其支持相似度匹配和可用于快速搜索的索引操作类，适合作为第一阶段站内搜索优化方案。参考: https://www.postgresql.org/docs/current/pgtrgm.html
- 智谱官方文档显示 `glm-4.6v-flash` 是免费多模态模型，支持视频、图像、文本和文件输入，接口使用 `https://open.bigmodel.cn/api/paas/v4/chat/completions`。参考: https://docs.bigmodel.cn/cn/guide/models/free/glm-4.6v-flash

### 2.4 已确认技术决策

1. 后端数据访问层使用 MyBatis-Plus，并接入 MyBatis-Plus 分页插件。
2. 后台 UI 采用 Google Material Design 3 设计语言，优先按 Material 组件、色彩、密度、状态和动效规范设计。
3. 文件存储第一阶段使用本机本地存储，存储根目录使用相对项目根目录的 `storage/uploads`，不接入 MinIO / S3。
4. 鉴权方案使用 JWT，本地 `.env` 已生成随机 JWT 密钥。
5. 搜索方案选择 PostgreSQL `pg_trgm` + GIN 索引，优先保证第一阶段搜索性能。
6. 第一阶段需要注册功能，同时内置管理员账号 `admin`。
7. 普通用户和管理员密码统一使用 BCrypt 哈希保存，哈希强度使用通用配置 `PASSWORD_BCRYPT_STRENGTH`。
8. 私密文章第一阶段实现，访问规则包括公开、仅自己、仅好友、仅选中好友、排除选中好友。
9. 前台与后台共用一个 Vue 3 + Vite 前端应用，通过路由和布局分区。
10. Docker 方案后置为独立阶段。中间件容器、后端容器、前端容器都归属于本项目部署方案，不依赖当前公共服务容器。
11. Git 工作流按约定使用 `codex/CreatorSpace` 分支。
12. AI 功能暂不进入第一阶段功能范围，但 `.env` 先预留智谱 AI 配置，默认多模态模型为 `glm-4.6v-flash`。

## 3. 产品目标

### 3.1 核心目标

1. 建立一个可长期维护的个人内容平台。
2. 支持文章、作品、灵感、页面配置和主题风格统一管理。
3. 提供漂亮、可切换风格的访客前台。
4. 提供完整后台 CMS，让内容创建、管理、统计和配置都可视化完成。
5. 预留高级能力扩展空间，例如 Redis 统计、文章版本、搜索系统、AI 辅助写作和部署自动化。

### 3.2 非目标

初版不追求一次性完成所有高级功能。以下内容可以放到后续阶段:

- AI 写作、AI 摘要、AI 标签生成。
- 全文搜索引擎独立部署，例如 MeiliSearch 或 Elasticsearch。
- WebSocket 实时通知。
- 多租户站点。
- 复杂会员体系或商业支付。

## 4. 用户角色

| 角色 | 说明 | 核心权限 |
| --- | --- | --- |
| 游客 | 未登录访客 | 浏览公开内容、搜索 |
| 注册用户 | 已注册访客 | 浏览公开内容、评论、回复、点赞、收藏 |
| 管理员 | 站点拥有者 | 管理所有内容、配置、主题、文件、评论和统计 |
| 编辑者 | 可选角色 | 管理文章、作品和创意卡片 |
| 友人 | 可选角色 | 查看作者按好友关系开放的私密内容 |

第一版实现游客、注册用户、好友关系和管理员。编辑者作为权限模型预留。

## 5. 前台访客端需求

### 5.1 首页

首页需要体现个人品牌和内容入口，建议包含:

- 头像、昵称、一句话介绍。
- 精选文章。
- 精选作品。
- 最近动态或创作时间线。
- 技术栈标签。
- 标签云。
- 访问统计摘要。
- 社交链接或联系方式。

首页内容应由后台配置，不建议硬编码在前端。

### 5.2 博客文章页

文章页需要支持:

- 文章列表。
- 分类筛选。
- 标签筛选。
- 关键词搜索入口。
- 推荐 / 置顶文章。
- 草稿不对外展示。
- 私密文章需要访问控制，第一阶段实现公开、仅自己、仅好友、仅选中好友、排除选中好友五种模式。

### 5.3 文章详情页

文章详情页需要支持:

- Markdown 渲染。
- 代码高亮。
- 文章目录。
- 封面图。
- 摘要。
- 分类与标签展示。
- 阅读量统计。
- 点赞。
- 评论列表。
- 上一篇 / 下一篇。

### 5.4 作品集页

作品集页用于展示个人项目、设计、摄影、视频或实验作品，需要支持:

- 作品列表。
- 类型筛选。
- 技术栈筛选。
- 推荐作品。
- 作品封面。
- GitHub 链接。
- Demo 链接。
- 视频链接。
- 展示 / 隐藏状态。

### 5.5 作品详情页

作品详情页需要支持:

- 项目简介。
- 技术栈标签。
- 项目截图。
- 开发过程。
- 遇到的问题与解决方案。
- 项目时间线。
- 外部链接。

### 5.6 创意墙

创意墙用于展示碎片灵感、Prompt、图片、UI 截图、短代码、链接或项目想法。页面形态建议接近瀑布流或 Gallery。

需要支持:

- 卡片列表。
- 卡片类型筛选。
- 标签筛选。
- 图片 / 文本 / Prompt / 代码 / 链接类型。
- 来源链接。
- 是否公开。

### 5.7 关于页

关于页需要支持:

- 个人介绍。
- 技能栈。
- 经历。
- 联系方式。
- 社交链接。
- 可选简历下载入口。

内容应优先来自后台页面配置。

### 5.8 留言板 / 评论互动

需要支持:

- 文章评论。
- 作品评论。
- 多级回复评论。
- 评论审核后展示。
- 评论点赞。

游客只有浏览权限，评论、回复、点赞和收藏都要求登录。第一阶段评论系统直接按完整多级评论设计，并接入敏感词表。

### 5.9 搜索页

搜索范围建议包括:

- 文章标题。
- 文章摘要。
- 文章内容。
- 作品标题。
- 作品描述。
- 创意卡片标题。
- 标签。
- 分类。

第一版使用 PostgreSQL `pg_trgm` 扩展配合 GIN 索引实现模糊搜索，避免普通 `%keyword%` 查询在数据量增长后频繁全表扫描。后续如果需要更强的分词、排序和跨字段相关性，再评估 PostgreSQL 全文搜索、MeiliSearch 或 Elasticsearch。

## 6. 后台 CMS 需求

### 6.1 登录与注册

后台需要独立登录入口。前台需要注册和登录能力，后台需要管理员登录能力。

需要支持:

- 用户名 / 密码登录。
- 用户注册，注册字段只包含用户名和密码。
- JWT 登录态。
- BCrypt 密码哈希。
- 普通用户和管理员共用 `PASSWORD_BCRYPT_STRENGTH` 控制哈希强度。
- 用户不需要邮箱字段，用户名允许后续修改。
- 内置管理员账号 `admin`。
- 管理员初始密码哈希从本地 `.env` 读取，源密码不写入仓库。
- 登录状态过期处理。
- 未登录时禁止访问后台接口。

### 6.2 仪表盘

后台首页需要提供运营概览:

- 文章总数。
- 作品总数。
- 评论总数。
- 今日访问量。
- 最近 7 天访问趋势。
- 热门文章 Top 5。
- 热门作品 Top 5。
- 最新留言 / 评论。
- 标签分布。

### 6.3 文章管理

需要支持:

- 新建文章。
- 编辑文章。
- 删除文章。
- 草稿保存。
- 发布文章。
- 取消发布。
- 定时发布, 后续阶段实现。
- 置顶文章。
- 推荐文章。
- 私密文章, 第一阶段实现公开、仅自己、仅好友、仅选中好友、排除选中好友。
- Markdown 编辑。
- 封面上传。
- 分类与标签绑定。
- 文章版本保存与回滚, 后续阶段实现。

### 6.4 分类与标签管理

分类需要支持:

- 新增、编辑、删除。
- 排序。
- 启用 / 禁用。
- 用于文章或作品的类型区分。

标签需要支持:

- 新增、编辑、删除。
- 标签颜色。
- 标签权重或排序。
- 文章、作品、创意卡片复用。

### 6.5 作品管理

需要支持:

- 新增作品。
- 编辑作品。
- 删除作品。
- 设置展示 / 隐藏。
- 设置推荐。
- 上传封面。
- 管理项目截图。
- 配置 GitHub、Demo、视频等链接。
- 维护技术栈。
- 维护项目详情 Markdown。

### 6.6 创意墙管理

需要支持:

- 新增创意卡片。
- 编辑创意卡片。
- 删除创意卡片。
- 设置公开 / 私密。
- 上传图片。
- 维护来源链接。
- 选择卡片类型。
- 绑定标签。

### 6.7 评论管理

需要支持:

- 查看评论列表。
- 按目标类型筛选。
- 审核通过。
- 驳回。
- 删除。
- 拉黑用户名 / IP, 后续阶段实现。
- 敏感词过滤。

### 6.8 主题管理

主题系统是本项目特色能力。后台需要支持:

- 查看主题列表。
- 切换当前主题。
- 编辑主题基础信息。
- 配置主色、背景、字体、卡片样式和布局类型。
- 保存扩展 JSON 配置。
- 预览主题效果, 后续阶段实现。

预设主题建议:

| 主题 | 风格 |
| --- | --- |
| Cyber Night | 暗色科技风 |
| Soft Blog | 温柔浅色风 |
| Pixel World | 像素复古风 |
| Minimal White | 极简白色风 |
| Glass Space | 玻璃星空风 |

### 6.9 页面配置

页面配置用于避免把个人信息硬编码在前端。

需要支持:

- 站点名称。
- 站点副标题。
- 头像。
- 首页介绍文案。
- 社交链接。
- SEO 标题。
- SEO 描述。
- 首页精选内容配置。
- 关于页内容。

### 6.10 文件资源管理

需要支持:

- 上传文件。
- 上传文章封面。
- 上传作品封面。
- 上传作品截图。
- 文件列表。
- 文件预览。
- 文件分类。
- 文件大小限制。
- 文件引用统计。
- 删除文章或作品时，自动删除未被任何内容引用的文件。
- 同一图片被多个内容使用时按不同文件资源保存，不做跨内容复用。
- 本机本地存储, 路径相对项目根目录, 第一阶段不接入对象存储。

### 6.11 访问统计

需要支持:

- 记录访问路径。
- 记录访问目标类型。
- 记录 IP。
- 记录 User-Agent。
- 记录 Referer。
- 统计 PV。
- 统计 UV。
- 热门文章。
- 热门作品。
- 最近 7 天 / 30 天趋势。

第一版可以先写入 PostgreSQL。后续使用 Redis 做计数缓冲，再定时同步到 PostgreSQL。

### 6.12 操作日志

后台关键操作需要留痕:

- 新增文章。
- 更新文章。
- 删除文章。
- 发布文章。
- 切换主题。
- 审核评论。
- 上传文件。

后续可以通过 AOP 注解统一记录操作日志。

## 7. 后端模块划分

建议后端按业务模块组织:

```text
src/main/java/com/creatorspace
├── CreatorSpaceApplication.java
├── common
│   ├── result
│   ├── exception
│   ├── enums
│   └── utils
├── config
│   ├── SecurityConfig.java
│   ├── RedisConfig.java
│   └── WebMvcConfig.java
│   └── MybatisPlusConfig.java
├── security
│   ├── JwtFilter.java
│   ├── JwtUtil.java
│   └── LoginUser.java
├── module
│   ├── auth
│   ├── user
│   ├── article
│   ├── category
│   ├── tag
│   ├── portfolio
│   ├── inspiration
│   ├── comment
│   ├── theme
│   ├── file
│   ├── search
│   ├── dashboard
│   └── visit
└── task
    ├── VisitSyncTask.java
    └── SchedulePublishTask.java
```

每个业务模块内部建议保持统一结构:

```text
controller
service
service/impl
mapper
entity
dto
vo
```

数据访问层确定为 MyBatis-Plus。分页统一通过 MyBatis-Plus 分页插件实现，列表接口不得手写不受控的全量查询后内存分页。Spring Boot 4.x 接入时需要确认 MyBatis-Spring 4.x、MyBatis-Plus Starter 和分页插件模块的版本兼容性。

## 8. 前端模块划分

前端确定为一个 Vue 3 + Vite 应用，按路由和布局区分前台与后台，不拆成两个前端项目。建议前端按前台与后台分区:

```text
src
├── app
├── router
├── stores
├── api
├── layouts
│   ├── public
│   └── admin
├── views
│   ├── public
│   └── admin
├── components
├── composables
├── styles
└── types
```

前台重点是内容展示和主题风格。后台重点是高效管理，不要做成营销页，应该偏信息密度高、可扫描、可重复操作的 CMS 工作台。

后台视觉与交互采用 Google Material Design 3 语言。后续实现时优先遵循 Material 的色彩系统、形状系统、状态层、输入控件、导航、表格密度和响应式布局规范。组件库或自研封装需要服务于这个设计目标，而不是再切换到 Element Plus / Naive UI 风格。

## 9. 核心数据表草案

第一版建议优先设计以下表:

| 表名 | 用途 |
| --- | --- |
| `users` | 用户 |
| `roles` | 角色 |
| `user_roles` | 用户角色关联 |
| `user_friendships` | 用户好友关系 |
| `articles` | 文章 |
| `article_visibility_users` | 文章好友可见性名单 |
| `article_versions` | 文章版本 |
| `categories` | 分类 |
| `tags` | 标签 |
| `article_tags` | 文章标签 |
| `portfolio_projects` | 作品 |
| `project_tags` | 作品标签 |
| `project_images` | 作品截图 |
| `inspiration_cards` | 创意卡片 |
| `comments` | 评论 |
| `theme_configs` | 主题配置 |
| `site_configs` | 页面与站点配置 |
| `file_resources` | 文件资源 |
| `file_resource_references` | 文件引用关系 |
| `visit_logs` | 访问日志 |
| `like_records` | 点赞记录 |
| `favorite_records` | 收藏记录 |
| `friend_links` | 友情链接 |
| `sensitive_words` | 敏感词 |
| `operation_logs` | 后台操作日志 |

表结构细节需要在数据库设计文档中单独展开，并结合 PostgreSQL 类型能力设计，例如 `jsonb`、数组、全文索引或扩展索引。

## 10. 核心接口草案

### 10.1 前台接口

```text
GET    /api/site/config
GET    /api/theme/current

GET    /api/articles
GET    /api/articles/{id}
GET    /api/articles/slug/{slug}
GET    /api/categories
GET    /api/tags

GET    /api/projects
GET    /api/projects/{id}

GET    /api/inspirations
GET    /api/search

POST   /api/comments
GET    /api/comments

POST   /api/likes
DELETE /api/likes/{id}
```

### 10.2 后台接口

```text
POST   /api/admin/auth/login

GET    /api/admin/dashboard/overview
GET    /api/admin/dashboard/visit-trend
GET    /api/admin/dashboard/hot-articles
GET    /api/admin/dashboard/hot-projects

POST   /api/admin/articles
PUT    /api/admin/articles/{id}
DELETE /api/admin/articles/{id}
PUT    /api/admin/articles/{id}/publish
PUT    /api/admin/articles/{id}/top

POST   /api/admin/projects
PUT    /api/admin/projects/{id}
DELETE /api/admin/projects/{id}

POST   /api/admin/inspirations
PUT    /api/admin/inspirations/{id}
DELETE /api/admin/inspirations/{id}

GET    /api/admin/comments
PUT    /api/admin/comments/{id}/approve
PUT    /api/admin/comments/{id}/reject

POST   /api/admin/files/upload
GET    /api/admin/files

GET    /api/admin/themes
PUT    /api/admin/themes/{id}/switch
PUT    /api/admin/site/config
```

接口路径只是初稿，后续需要根据 REST 风格、权限边界和前端页面实际需求调整。

## 11. 分阶段范围

### 11.1 第一阶段: 基础可运行版

目标: 前后端项目能完整跑起来，并具备核心内容发布能力。

建议范围:

- 后端项目初始化。
- 前端项目初始化。
- 单个 Vue 应用同时承载前台与后台。
- PostgreSQL 连接。
- Redis 连接预留或健康检查。
- MyBatis-Plus 接入。
- MyBatis-Plus 分页插件接入。
- JWT 鉴权。
- 用户注册。
- 内置管理员初始化。
- 管理员登录。
- 文章发布。
- 文章列表。
- 文章详情。
- 私密文章。
- 分类与标签。
- 作品展示。
- 文件上传基础能力。
- 本机本地文件存储, 存储根目录为相对路径 `storage/uploads`。
- PostgreSQL `pg_trgm` 搜索。
- 后台文章 / 作品管理。

不建议第一阶段加入:

- AI。
- 独立搜索引擎。
- 多角色复杂权限。
- WebSocket。
- 完整访问统计同步。
- Docker 部署方案。

### 11.2 第二阶段: 完整内容平台

目标: 从基础博客扩展为内容展示与互动平台。

建议范围:

- 评论系统。
- 留言板。
- 主题切换。
- 页面配置。
- 数据看板。
- 访问统计。
- 作品详情页。
- 创意墙。
- 友情链接。

### 11.3 第三阶段: 后端技术亮点

目标: 增强项目技术含量和可讲述性。

建议范围:

- Redis 热门文章缓存。
- 访问计数 Redis 缓冲与定时同步。
- 定时发布。
- AOP 操作日志。
- 文章版本管理。
- 私密文章。
- PostgreSQL 全文搜索或独立搜索引擎。

### 11.4 第四阶段: AI / 全栈加分项

目标: 让平台具备更强的创作辅助能力。

建议范围:

- AI 生成摘要。
- AI 生成标签。
- AI 优化标题。
- AI 生成作品介绍。
- AI 搜索问答。
- WebSocket 通知。

AI 环境变量在第一阶段先预留，功能实现放到第四阶段。当前默认大模型供应商为智谱 AI，多模态模型配置为 `ZHIPU_MULTIMODAL_MODEL=glm-4.6v-flash`，API Key 只允许写入本地 `.env` 或部署密钥系统。

### 11.5 部署阶段: Docker 独立方案

目标: 为 CreatorSpace 设计独立部署方案。

原则:

- 当前公共服务容器只作为开发环境参考，不作为项目最终部署依赖。
- PostgreSQL、Redis、后端、前端都归属于 CreatorSpace 自己的 Docker 方案。
- Docker 配置在中后期单独设计和验证，不阻塞第一阶段功能开发。
- 部署文档需要明确 Windows PowerShell 下的启动、停止、查看日志和数据保留方式。

## 12. 非功能需求

### 12.1 安全

- 后台接口必须鉴权。
- 管理员密码必须加密存储。
- 普通用户和管理员密码必须使用同一套 BCrypt 强度配置。
- JWT 密钥必须从 `.env` 或部署密钥配置读取。
- `.env` 必须加入 `.gitignore`，不得提交真实密钥。
- 智谱 API Key 等第三方密钥不得提交到仓库。
- 文件上传必须校验大小和类型。
- 评论内容需要防 XSS。
- Markdown 渲染需要处理 HTML 注入风险。
- 不在 README、日志、提交信息中暴露密钥。

### 12.2 性能

- 首页和文章列表需要分页。
- 列表分页统一使用 MyBatis-Plus 分页插件。
- 站内搜索优先使用 PostgreSQL `pg_trgm` + GIN 索引。
- 热门内容统计后续使用 Redis 降低数据库压力。
- 文件资源需要合理缓存。
- 后台数据看板需要避免每次全表扫描。

### 12.3 可维护性

- 前后端类型定义清晰。
- 后端统一响应结构。
- 后端统一异常处理。
- 后端模块边界清晰。
- 前端 API 调用集中管理。
- 文档中的命令必须适配 Windows PowerShell。

### 12.4 可观测性

- 后端需要基础日志。
- 关键后台操作需要操作日志。
- 后续可加入健康检查接口。
- 后续可加入接口耗时统计。

## 13. 验收标准

### 13.1 第一阶段验收

- 可以在 Windows 11 + PowerShell 环境启动后端。
- 可以在 Node 24 环境启动前端。
- 后端可以连接 PostgreSQL。
- 后端可以连接或检查 Redis。
- 用户可以注册。
- 管理员可以登录后台。
- 管理员可以新增、编辑、发布文章。
- 管理员可以创建私密文章。
- 未授权用户无法直接查看私密文章内容。
- 前台可以查看文章列表和详情。
- 搜索接口可以通过 `pg_trgm` 索引检索文章、作品或创意内容。
- 管理员可以新增作品。
- 前台可以查看作品列表。
- 文件上传可用，并保存到项目根目录下的相对路径 `storage/uploads`。
- README 说明启动方式、环境变量和基础命令。

### 13.2 第二阶段验收

- 前台主题可由后台切换。
- 首页内容可由后台配置。
- 评论提交后进入审核流程。
- 后台仪表盘展示真实统计。
- 创意墙可以展示公开卡片。

### 13.3 第三阶段验收

- Redis 计数与数据库同步逻辑可验证。
- 定时发布能按发布时间发布文章。
- 文章编辑会保存版本。
- 操作日志能记录后台关键动作。
- 搜索能覆盖文章、作品和创意卡片。

## 14. 后续待细化问题

以下问题不再阻塞第一阶段方向，但需要在架构设计或实施计划中继续细化:

1. MyBatis-Plus、MyBatis-Spring、Spring Boot 4.x 的具体版本组合。
2. 后台 Material Design 3 的具体组件实现方式，是优先使用 Google Material Web 组件、自研封装，还是少量引入兼容库。
3. 本机文件存储的目录结构、访问 URL 映射和未引用文件清理实现细节。
4. JWT 的过期时间、刷新策略、登出策略和黑名单策略。
5. `pg_trgm` 索引覆盖哪些字段，以及不同内容类型的搜索排序规则。
6. 好友申请、同意、拒绝和拉黑的交互细节。
7. 私密文章好友名单在前端选择时的搜索、分页和批量操作细节。
8. Docker 阶段的数据卷、备份、迁移、日志和环境变量管理方案。
9. AI 和 pgvector 正式接入时的向量维度、模型选择、调用限流、内容安全、失败降级和费用控制方案。

## 15. 环境变量规划

本项目使用 `.env` 保存本机开发配置和敏感配置。真实 `.env` 不提交仓库，提交 `.env.example` 作为模板。

已预留的配置分类:

- 应用运行: `APP_ENV`、`SPRING_PROFILES_ACTIVE`、`SERVER_PORT`。
- 前后端地址: `WEB_BASE_URL`、`API_BASE_URL`、`CORS_ALLOWED_ORIGINS`。
- 数据库: `POSTGRES_HOST`、`POSTGRES_PORT`、`POSTGRES_DB`、`POSTGRES_USERNAME`、`POSTGRES_PASSWORD`。
- Redis: `REDIS_HOST`、`REDIS_PORT`、`REDIS_DATABASE`、`REDIS_PASSWORD`。
- JWT: `JWT_SECRET`、`JWT_ACCESS_TOKEN_EXPIRE_MINUTES`、`JWT_REFRESH_TOKEN_EXPIRE_DAYS`。
- 密码哈希: `PASSWORD_BCRYPT_STRENGTH`。
- 管理员初始化: `ADMIN_USERNAME`、`ADMIN_PASSWORD_HASH`。
- 文件存储: `FILE_STORAGE_TYPE`、`LOCAL_FILE_STORAGE_ROOT`、`LOCAL_FILE_PUBLIC_PREFIX`、`UPLOAD_MAX_FILE_SIZE_MB`、`UPLOAD_MAX_REQUEST_SIZE_MB`。
- AI 预留: `AI_ENABLED`、`AI_PROVIDER`、`ZHIPU_API_BASE_URL`、`ZHIPU_API_KEY`、`ZHIPU_MULTIMODAL_MODEL`、`ZHIPU_TEXT_MODEL`、`ZHIPU_REQUEST_TIMEOUT_SECONDS`、`ZHIPU_MAX_OUTPUT_TOKENS`、`ZHIPU_THINKING_ENABLED`。
