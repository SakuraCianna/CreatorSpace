@startuml
title 功能流程 05 - 留言板完整流程

|访客|
:进入留言板页面;
:浏览已公开留言列表;
:填写留言表单;
if (前端校验?) then (通过)
  :提交留言;
else (不通过)
  :修正后重新填写;
endif

|系统|
:接收留言请求;
:去除 XSS 风险字符;
:匹配敏感词库;
if (含敏感词?) then (是)
  :标记敏感标记;
endif
:写入数据库 状态 PENDING;

|管理员|
:登录后台;
:进入留言管理模块;
:查看待审核留言列表;
:查看留言详情;
if (审核通过?) then (批准)
  :批准留言 状态 APPROVED;
else (驳回)
  :驳回或删除留言;
endif

|系统|
:更新留言状态;
:记录操作日志;
:前端刷新展示;

|访客|
if (审核通过?) then (是)
  :留言公开展示;
else (否)
  :留言不可见;
endif

stop
@enduml
