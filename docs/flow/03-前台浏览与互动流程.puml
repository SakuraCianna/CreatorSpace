@startuml
title 功能流程 03 - 前台浏览与互动流程

|访客|
:访问首页;
:导航至列表页;
:浏览列表内容;
:点击条目查看详情;
:渲染正文内容;
:展示互动面板;

if (用户想互动?) then (是)
  if (已登录?) then (是)
    if (点赞/收藏?) then (是)
      :执行点赞或收藏;
    else (写评论)
      :填写评论内容;
      :提交评论;
    endif
  else (未登录)
    :跳转登录页;
    :登录后继续操作;
  endif
endif

|后端|
if (请求类型?) then (列表/详情)
  :查询数据库;
  :返回内容数据;
else (互动操作)
  :校验用户 Token;
  :处理点赞/收藏/评论;
endif
:更新统计计数;
:记录访问日志;

stop
@enduml
