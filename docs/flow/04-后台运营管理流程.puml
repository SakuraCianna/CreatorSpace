@startuml
title 功能流程 04 - 后台运营管理流程

|管理员|
:登录后台;
:进入运营概览看板;
:查看数据统计与趋势;

:从侧边栏选择管理模块;
:执行审核或配置操作;
if (操作类型?) then (内容审核)
  :查看待审核内容详情;
  if (审核通过?) then (批准)
    :批准内容 标记推荐;
  else (驳回)
    :填写驳回意见;
  endif
else (配置管理)
  :配置主题/站点/规则;
endif

|系统|
:处理请求;
:写入操作审计日志;
:更新缓存;
:前端同步;

stop
@enduml
